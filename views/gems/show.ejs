<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <link rel="stylesheet" href="/stylesheets/edit.css" />
  <title><%= gem.name %></title>
</head>
<body>

  <%- include('../partials/_navbar.ejs') %>
  <section class="options">
    <section class="greetings">
      <h1><%= gem.name %></h1>
      <p>"Each gem in my collection has been selected with great care over many years. 
        Every crystal offers a unique effect on my aura and senses, contributing something 
        special to my energy. Over the past decade, 
        I’ve cultivated a deep sensitivity to connect with each stone, 
        allowing me to fully appreciate its individual resonance."</p>
    </section>

    <div class="edit-group">
      <a href="/users/<%= user._id %>/gems/<%= gem._id %>/edit">Edit this Gem</a>
    </div>

    <div class="delete-group">
      <form action="/users/<%= user._id %>/gems/<%= gem._id %>?_method=DELETE" method="POST" onsubmit="return confirmDelete()">
        <button type="submit">Delete this gem from my collection</button>
      </form>
    </div>

    <div class="back"> 
      <a href="/">Back to collection</a>
    </div>

  </section>

  <script>
    function confirmDelete() {
      return confirm("Are you sure you want to delete this gem from your collection?");
    }
  </script>

</body>
</html>
